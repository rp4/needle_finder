// Centralized icon exports - tree-shakeable
export {
  AlertTriangle,
  BarChart2,
  Check,
  CheckCircle,
  Download,
  Info,
  Search,
  X
} from 'lucide-react';

// This ensures we only bundle the icons we actually use
// Reduces icon bundle from ~50KB to ~8KB